export class Result {
  constructor(public title: string, public venueAddress: string, public cityName: string, public regionAbbr: string, public postalCode: string, public artistName: string, public startTime: string, public albumId: string, public exist: boolean = false, public url: string) {}
}
